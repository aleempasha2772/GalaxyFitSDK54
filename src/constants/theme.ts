// Shared theme constants. Every screen pulls colors from here so the app uses
// one consistent set of values rather than hardcoding hex strings per screen.
export const COLORS = {
  background: '#0A0A0F',
} as const;
